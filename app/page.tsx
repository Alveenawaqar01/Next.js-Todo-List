import TodoList from "@/components/ui/todo-list"; // Capitalize the imported component name

const TodoListPage = () => {
  return (
    <div>
      <TodoList /> {/* Capitalize the component name when used in JSX */}
    </div>
  );
};

export default TodoListPage;

